# yaml-language-server: $schema=https://raw.githubusercontent.com/Open-CMSIS-Pack/devtools/main/tools/projmgr/schemas/clayer.schema.json

layer:
  type: Board
  components:
    - component: Board:Test:Rev1
  
  connections:
    - connect: provided connections A and B
      set: set1.select1
      info: set 1 select 1
      provides:
        - connectA
        - connectB

    - connect: provided connections B and C
      set: set1.select2
      info: set 1 select 2
      provides:
        - connectB
        - connectC